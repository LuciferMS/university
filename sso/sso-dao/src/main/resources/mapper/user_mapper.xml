<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.university.sso.dao.mapper.UserMapper">

    <resultMap id="SimpleUserMapper" type="com.university.commen.domain.sso.User">

    </resultMap>

    <!-- 查询条件拼接sql -->
    <sql id="SimpleSelectSql">

    </sql>

    <insert id="saveBean">
        INSERT INTO user(id,username,password)
        VALUES(#{id}, #{username} ,#{password} );
    </insert>

    <select id="selectBean">

    </select>

</mapper>